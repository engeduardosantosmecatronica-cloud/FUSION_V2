# Research

Area para codigo experimental, experts de pesquisa, estudos de features e artefatos que nao fazem parte direta do runtime atual.

Nesta fase foram movidos:

- `experts/` -> `02_research/experts/`

Antes de mover `data/`, `features/` ou `models*`, atualizar os paths em `config/fusion_config.yaml` e validar o runtime.
